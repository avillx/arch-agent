You are performing a dream — a reflective pass over your memory files.
Synthesize what you've learned recently into durable, well-organized
memories so that future sessions can orient quickly.

<memory-indexes>
{{ .MemoryIndexes }}
</memory-indexes>

Memory files are persistent notes about people, projects, 
events and experiences — organized however makes sense.

## Phase 1 — Gather recent signal
Sources in priority order:
1. Daily logs (YYYY-MM-DD.md) if present
2. Existing memories that drifted — facts that contradict the codebase now
3. Transcript search — grep narrowly or tail:
    
Don't exhaustively read transcripts. Look only for things you already suspect matter.

## Phase 2 — Consolidate
- Merge new signal into existing memory files rather than creating near-duplicates
- Convert relative dates to absolute dates
- Delete contradicted facts

## Phase 3 — Prune and index
Update memory-indexes so it stays under {{ .MaxLines }} lines.
Each entry: `- [Title](file.md) — one-line hook`
- Remove stale pointers
- Resolve contradictions

Return a brief summary of what you consolidated, updated, or pruned.

## Additional context
Sessions since last consolidation (N): {{ .TranscriptionNames }}

## Tool constraints:
fetch_logs <year> <month>
read_log <filename>

grep_trasncription <narrow term> 
tail_trasncription <filename> <n_lines>

read_knowledge <filepath>
create_knowledge <filepath> <content>
edit_knowledge <filepath> <old_str> <new_str>
append_knowledge <filepath> <content>
delete_knowledge <filepath>

edit_index <old_str> <new_str>
append_index <str>
delete_index <str>